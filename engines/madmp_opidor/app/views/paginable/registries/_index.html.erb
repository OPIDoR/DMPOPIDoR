<div class="table-responsive">
  <table class="table table-hover" id="guidance-groups">
    <thead>
      <tr>
        <th scope="col"><%= "#{_('Name')} (#{_('Version')})" %>&nbsp;<%= paginable_sort_link('registries.name') %></th>
        <th scope="col"><%= _('Description') %></th>
        <th scope="col"><%= _('URI') %></th>
        <th scope="col"><%= _('Category') %></th>
        <th scope="col"><%= _('Data Types') %></th>
        <th scope="col" class="sorter-false"><span aria-hidden="false" class="sr-only"><%= _('Actions') %></span></th>
      </tr>
    </thead>
    <tbody>
      <% scope.each do |registry| %>
        <tr>
          <td><%= "#{registry.name} (#{registry.version})" %></td>
          <td><%= registry.description %></td>
          <td><%= registry.uri %></td>
          <td><%= registry.category %></td>
          <td><%= registry.data_types.join(", ") %></td>
          <td>
            <div class="dropdown">
              <button
                class="btn btn-link dropdown-toggle"
                type="button"
                id="org-<%= registry.id %>-actions"
                data-bs-toggle="dropdown"
                aria-haspopup="true"
                aria-expanded="true">
                <%= _('Actions') %><span class="caret"></span>
              </button>
              <ul class="dropdown-menu" aria-labelledby="org-<%= registry.id %>-actions">
                <li class="nav-item"><%= link_to _('Edit'), edit_super_admin_registry_path(registry), class: 'dropdown-item px-3' %></li>
                <li class="nav-item">
                  <%= link_to(_('Delete'), super_admin_registry_path(registry), class: 'dropdown-item px-3', rel: 'nofollow', 'data-method': 'delete',
                    'data-confirm': _("Are you sure you want to delete the registry \"%{title}\"?") % { title: registry.name }) %>
                </li>
              </ul>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
