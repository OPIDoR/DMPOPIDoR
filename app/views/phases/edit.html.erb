<%# locals: { plan, phase, readonly, question_guidance } %>
<% title "#{plan.title} - Write plan" %>
<div class="row">
  <div class="col-md-12">
    <h1><%= plan.title %></h1> 
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <% if Dataset.table_exists? %>
      <%= render partial: 'phases/edit_plan_answers_datasets', layout: 'plans/navigation', locals: local_assigns %>
    <% else %>
      <%= render partial: 'phases/edit_plan_answers', layout: 'plans/navigation', locals: local_assigns %>
    <% end %>
  </div>
</div>
