<turbo-stream action="append" target="notification-area">
  <template>
    <script>
      renderNotice("<%= _("The credentials have been sent to %{email}.") % { email: @api_client.contact_email } %>");
      toggleSpinner(false);
    </script>
  </template>
</turbo-stream>
